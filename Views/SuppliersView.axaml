<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:PharmacyWarehouse.ViewModels.Pages"
             mc:Ignorable="d"
             x:Class="PharmacyWarehouse.Views.SuppliersView"
             x:DataType="vm:SuppliersViewModel">

    <DockPanel Margin="15">
        <Button DockPanel.Dock="Top" 
                Content="➕ Добавить поставщика" 
                HorizontalAlignment="Left" 
                Margin="0,0,0,15"
                Click="AddSupplier_Click" 
                Height="42" 
                Width="280" />

        <DataGrid ItemsSource="{Binding Suppliers}" 
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     GridLinesVisibility="All"
                     CanUserResizeColumns="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="320"/>
                <DataGridTextColumn Header="ИНН" Binding="{Binding Inn}" Width="140"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="160"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="300"/>
                <DataGridTextColumn Header="Банк" Binding="{Binding Bank}" Width="180"/>
                <DataGridTextColumn Header="Расчётный счёт" Binding="{Binding AccountNumber}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>