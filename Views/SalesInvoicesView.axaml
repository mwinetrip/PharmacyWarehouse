<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:PharmacyWarehouse.ViewModels.Pages"
             mc:Ignorable="d"
             x:Class="PharmacyWarehouse.Views.SalesInvoicesView"
             x:DataType="vm:SalesInvoicesViewModel">

    <DockPanel Margin="15">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="10" Margin="0,0,0,15">
            <Button Content="➕ Выписать счёт-фактуру" 
                    Click="AddSales_Click" 
                    Height="42" Width="280" />

            <Button Content="🗑 Удалить выбранное" 
                    Command="{Binding DeleteCommand}"
                    Height="42" Width="200"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding SalesInvoices}" 
                     SelectedItem="{Binding SelectedSales}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     GridLinesVisibility="All"
                     CanUserResizeColumns="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="№ счёта" Binding="{Binding InvoiceNumber}" Width="150"/>
                <DataGridTextColumn Header="Дата выписки" Binding="{Binding IssueDate, StringFormat=dd.MM.yyyy}" Width="140"/>
                <DataGridTextColumn Header="Покупатель" Binding="{Binding Customer.Name}" Width="280"/>
                <DataGridTextColumn Header="Кол-во позиций" Binding="{Binding Items.Count}" Width="110"/>
                <DataGridTextColumn Header="Общая сумма" Binding="{Binding TotalAmount, StringFormat='0.00 ₽'}" Width="140"/>
                <DataGridTextColumn Header="Продавец" Binding="{Binding SellerName}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>